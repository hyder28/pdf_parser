version: 2.1

orbs:
  heroku: circleci/heroku@1.2.6

jobs:
  deploy:
    executor: heroku/default
    steps:
      - checkout
      - run:
          command: >
            echo "Run python unit tests"
      - heroku/deploy-via-git

workflows:
  heroku_deploy:
    jobs:
      - deploy
